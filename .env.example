# eBay developer keyset (developer.ebay.com). Use the Sandbox keyset until E2 passes.
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
EBAY_REFRESH_TOKEN=
# sandbox | production  (production only after a successful sandbox end-to-end publish)
EBAY_ENV=sandbox
